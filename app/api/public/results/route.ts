import { getPublishedResults } from "@/lib/database";

export async function GET(request:Request){const url=new URL(request.url);const all=await getPublishedResults();const hasOfficial=all.some((item)=>!item.isDemo);const includeDemo=url.searchParams.get("includeDemo")==="true"||(!hasOfficial&&url.searchParams.get("includeDemo")!=="false");const results=all.filter((item)=>(!url.searchParams.get("technology")||item.technologyCode===url.searchParams.get("technology"))&&(!url.searchParams.get("indicator")||item.indicatorCode===url.searchParams.get("indicator"))&&(!url.searchParams.get("level")||item.evidenceLevel===url.searchParams.get("level"))&&(!url.searchParams.get("lake")||item.lake===url.searchParams.get("lake"))&&(includeDemo||!item.isDemo));return Response.json({data:results,meta:{count:results.length,source:hasOfficial&&!includeDemo?"official":"with-demo"}})}
